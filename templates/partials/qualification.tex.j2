\chapter{Infrastructure de qualification}

\BLOCK{ if qualification.mode == "ha_mirror" }
Cette infrastructure de qualification reprend la même répartition de
charge (HA) que la production, à taille réduite~: elle permet de
valider les procédures de bascule/mise à jour dans des conditions
structurellement proches de la production.
\BLOCK{ else }
Cette infrastructure de qualification est volontairement minimale~: une
VM regroupe l'ensemble des rôles de la zone publique (DMZ), une VM
regroupe les services LAN (annuaire, base de données, coordination), et
une VM assure le rôle de mailstore.
\BLOCK{ endif }

\section{Dimensionnement de l'infrastructure de qualification}

{\small
\renewcommand{\arraystretch}{1.3}
\begin{longtable}{|p{3.8cm}|p{0.9cm}|p{2.6cm}|p{0.7cm}|p{1cm}|p{1.1cm}|p{1.1cm}|p{1.1cm}|}
\hline
\rowcolor{primary}
\tblhead{Nœud} & \tblhead{Zone} & \tblhead{Composants} & \tblhead{vCPU} & \tblhead{RAM (Go)} & \tblhead{OS (Go)} & \tblhead{Appli (Go)} & \tblhead{Store (Go)} \\
\hline
\endhead
\BLOCK{ for n in qualif_nodes }
\VAR{ n.id } & \VAR{ n.zone } & \VAR{ n.components_display } & \VAR{ n.sizing.vcpu } & \VAR{ n.sizing.ram_gb } & \VAR{ n.sizing.disk_os_gb } & \VAR{ n.sizing.disk_appli_gb } & \BLOCK{ if n.sizing.disk_store_gb }\VAR{ n.sizing.disk_store_gb }\BLOCK{ else }---\BLOCK{ endif } \\
\hline
\BLOCK{ endfor }
\rowcolor{lightbg}
\textbf{Total (\VAR{ qualif_nodes|length } nœuds)} & & & \textbf{\VAR{ qualif_totals.vcpu }} & \textbf{\VAR{ qualif_totals.ram_gb }} & \textbf{\VAR{ qualif_totals.disk_os_gb }} & \textbf{\VAR{ qualif_totals.disk_appli_gb }} & \textbf{\VAR{ qualif_totals.disk_store_gb }} \\
\hline
\end{longtable}
}

\BLOCK{ if document_scope.schemas_architecture }
\section{Schéma d'architecture de qualification}
\begin{center}
\begin{adjustbox}{max width=\textwidth, max totalheight=0.5\textheight, center}
\VAR{ diagram_tikz_raw_qualif }
\end{adjustbox}
\end{center}
\BLOCK{ endif }
